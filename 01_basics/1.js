const accountID = "12345"   // Only way to make cnstant in js
let accountEmail = "abc@12345"
var accountPassword = "hello"

accountCity = "Jaipur"  // No keyword considered as var

// accountID = "9"  // Not allowed
accountEmail = "vaibhav"
accountPassword = "what"
accountCity = "Bengaluru"
let name;   // undefined

console.log(accountID)
console.table([accountEmail, accountPassword, accountCity, name])

// const hello; // Not allowed, const must be initialised